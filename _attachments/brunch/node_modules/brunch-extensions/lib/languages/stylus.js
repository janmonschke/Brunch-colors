(function() {
  var BaseLanguage, nib, stylus,
    __hasProp = Object.prototype.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

  nib = require('nib');

  stylus = require('stylus');

  BaseLanguage = require('./base').BaseLanguage;

  exports.StylusLanguage = (function(_super) {

    __extends(StylusLanguage, _super);

    function StylusLanguage() {
      StylusLanguage.__super__.constructor.apply(this, arguments);
    }

    StylusLanguage.prototype.compile = function(path, callback) {
      var _this = this;
      return this.readFile(path, function(error, data) {
        var compiler, _ref;
        if (error != null) return callback(error);
        return compiler = stylus(data).set('compress', true).set('firebug', !!((_ref = _this.config.stylus) != null ? _ref.firebug : void 0)).include(_this.getRootPath('app', 'styles')).use(nib()).render(callback);
      });
    };

    return StylusLanguage;

  })(BaseLanguage);

}).call(this);
