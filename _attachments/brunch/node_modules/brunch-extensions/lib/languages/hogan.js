(function() {
  var BaseLanguage, hogan,
    __hasProp = Object.prototype.hasOwnProperty,
    __extends = function(child, parent) { for (var key in parent) { if (__hasProp.call(parent, key)) child[key] = parent[key]; } function ctor() { this.constructor = child; } ctor.prototype = parent.prototype; child.prototype = new ctor; child.__super__ = parent.prototype; return child; };

  hogan = require('hogan.js');

  BaseLanguage = require('./base').BaseLanguage;

  exports.HoganLanguage = (function(_super) {

    __extends(HoganLanguage, _super);

    function HoganLanguage() {
      HoganLanguage.__super__.constructor.apply(this, arguments);
    }

    HoganLanguage.prototype.compile = function(path, callback) {
      var _this = this;
      return this.readFile(path, function(error, data) {
        var content;
        if (error != null) return callback(error);
        try {
          content = hogan.compile(data, {
            asString: true
          });
          return callback(null, "exports.render = function(data) {          var t = new Hogan.Template();          t.r = " + content + ";          return t.render(data);        }");
        } catch (error) {
          return callback(error);
        }
      });
    };

    return HoganLanguage;

  })(BaseLanguage);

}).call(this);
